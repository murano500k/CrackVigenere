apply plugin: 'application'
mainClassName = "com.murano500k.crackvigenere.Main"


run {
    if (project.hasProperty("appArgs")) {
        args Eval.me(appArgs)
    }
}
dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.12'
}

